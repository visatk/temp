{
  "name": "temp",
  "version": "1.0.0",
  "description": "Stateful TempMail Telegram Bot utilizing Cloudflare Agents and V8 Isolate Native APIs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false",
    "cf-typegen": "wrangler types",
    "format": "prettier --write '**/*.{ts,js,json,md}'"
  },
  "dependencies": {
    "agents": "^0.5.1",
    "postal-mime": "^2.7.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.100.0",
    "prettier": "^3.3.0"
  }
}
